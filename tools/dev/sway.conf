# Minimal headless compositor for rendering the shell.
# wlroots gives us wlr-layer-shell, which is what the Island needs and what
# the target (KWin) also implements.
output HEADLESS-1 resolution 1280x800 position 0,0
default_border none
default_floating_border none
xwayland disable
