# Day-one verification — StarLite, 2026-09-03

Run from `tools/day-one-check.sh` over SSH inside the live Plasma session (`fedora.local`, 192.168.1.234, user adamrel). Raw output below; the manual `[YOU]` items are still open.

```
Quickshell StarLite — day-one verification
2026-09-03T16:09:56-04:00   host=fedora   user=adamrel
log: /home/adamrel/specs/day-one-20260903T160956.log
read-only: this script changes nothing

=== A. Hardware and session facts ===

-- A1. System --
  [info] model:   StarLite
  [info] os:      Fedora Linux 44 (KDE Plasma Desktop Edition)
  [info] kernel:  6.19.10-300.fc44.x86_64
  [info] session: type=wayland desktop=unset
  [PASS] Wayland session
  [info] plasma:  6.6.4
  [PASS] Plasma >= 6.6 — clears the ext-session-lock threshold (parent §6.2)

-- A2. Display — records the numbers every spec sizes against --
  Output: 1 eDP-1 3146cfb5-1a0b-4416-97e9-7e48cc7720ea
  	enabled
  	Geometry: 0,0 1543x1029
  	Scale: 1.4
  [YOU]  record panel resolution + the scale factor tablet mode picks (build-order §2.1)

-- A3. Inputs the design depends on --
  [PASS] touchscreen present
  [PASS] power button present — island-core §9.1 mechanism 1 is available
  [PASS] IIO sensors present: iio:device0 
  [info]   iio:device0: mxc4005
  [PASS] iio-sensor-proxy active

-- A4. Fingerprint — could erase the lock screen's worst hazard --
  [PASS] fingerprint reader available — lock-greeter §10 q6: a reader or PIN sidesteps the OSK hazard entirely

-- A5. Battery and the charge-cap trap (status-capsule §4) --
  [info] battery: BAT0  capacity=52%  status=Discharging
  [info] no charge cap set (or 100%)
        state:               discharging
        time to empty:       3.6 hours
        percentage:          52%
  [YOU]  plug in AC while at the cap and re-run — does UPower report 'charging' or something else? (status-capsule §11 q2)

=== B. The four gates (build-order §2.2) ===

-- B1. OSK on a Quickshell surface — gates launcher, polkit, lock screen --
  [FAIL] no virtual keyboard set — System Settings > Keyboard > Virtual Keyboard. Without one the probe proves nothing
  [WARN] quickshell not installed yet — install, then run the probe

-- B2. Notification bus name — gates notifications + CC notification list --
  [info] org.freedesktop.Notifications owned by pid 4051 (plasmashell)
  [WARN] owned by plasmashell, as expected — notifications §0: test whether a Quickshell NotificationServer can take it
  [YOU]  run a minimal Quickshell NotificationServer, re-check this, then: notify-send probe 'who renders this?'
  [YOU]    WRITE DOWN how to re-enable Plasma's before disabling anything (notifications §0)

-- B3. Polkit agent — gates polkit only, expected clean --
  [PASS] plasma-polkit-agent.service exists (state: active) — stoppable independently of plasmashell
  [info]   stop:   systemctl --user stop plasma-polkit-agent.service
  [info]   revert: systemctl --user start plasma-polkit-agent.service

-- B4. Quickshell version — polkit needs >= 0.3.0 --
  [FAIL] quickshell not installed — dnf copr enable errornointernet/quickshell && dnf install quickshell

=== C. Quick confirmations (build-order §2.3) ===

-- C1. Tooling the specs depend on --
  [WARN] wallust missing
  [PASS] plasma-apply-colorscheme
  [PASS] plasma-apply-wallpaperimage
  [WARN] qdbus missing
  [PASS] busctl
  [PASS] notify-send
  [info]   wallust: check 'dnf search wallust' / COPR, else cargo install (theming §12 q5)

-- C2. Plasma's own OSD — will duplicate yours (osd §1) --
  [WARN] no [OSD] Enabled key found — may be default-true and unset, or moved. osd §12 q2

-- C3. Shortcut collisions --
  [WARN] Ctrl+Alt+Del already bound — unbind before claiming it (power-menu §9 q1)
    Halt Without Confirmation=,,Shut Down Without Confirmation
    Lock Session=Meta+L\tScreensaver,Meta+L\tScreensaver,Lock Session
    Log Out=Ctrl+Alt+Del,Ctrl+Alt+Del,Show Logout Screen
  [WARN] Alt+D also appears bound — check before claiming
  [WARN] Alt+T also appears bound — check before claiming

-- C4. D-Bus interfaces the specs call into --
  [WARN] qdbus not found — install qt6-qttools

-- C5. System tray reload behaviour (system-tray §2 hypothesis) --
  [PASS] StatusNotifierWatcher owned by pid 4005 (kded6)
  [info]   if plasmashell owns it, a qs restart should only lose the HOST — the tray should repopulate
  [YOU]  with tray apps running, restart qs and confirm icons return (system-tray §10 q1)

-- C6. Tablet mode — THE PREMISE (build-order §2.1) --
  [YOU]  DETACH THE FOLIO and confirm ALL of:
  [YOU]     [ ] render scaling increases
  [YOU]     [ ] screen auto-rotates
  [YOU]     [ ] on-screen keyboard appears in a normal app (e.g. a text field in Dolphin)
  [YOU]     [ ] touch input works
  If these do not hold, STOP and re-read parent §6 before writing anything.

=== Summary ===
  pass: 13   fail: 2   manual steps: 10

Findings — paste into the specs that ask you to record them:

  Day-one verification, 2026-09-03:
    - Plasma 6.6.4 (>=6.6, session-lock OK)
    - power button: present
    - fingerprint: PRESENT — revisit lock-greeter §3.1
    - charge cap: none
    - notification name: held by plasmashell (pid 4051) — gate UNTESTED
    - polkit agent: plasma-polkit-agent.service, active — clean gate
    - plasma OSD key: not found — verify osd §1 fallback
    - Ctrl+Alt+Del: ALREADY BOUND — unbind first
    - SNI watcher: pid 4005 (kded6)

Still to do by hand: the 10 items marked [YOU] above.
Next: build-order §3 (architecture) once §2.1 holds.

log saved: /home/adamrel/specs/day-one-20260903T160956.log
```

## Follow-up the same evening

- Premise (§2.1) confirmed by Stephen with the folio detached: scaling up, auto-rotate, OSK in a normal app, touch — all hold.
- `dnf upgrade` (1,075 packages: kernel 6.19 → 7.1.12, Plasma 6.6.4 → 6.7.4; needed `--allowerasing` for the `kmime` → `kf6-kmime` split), reboot, `quickshell` 0.3.1 from the errornointernet COPR, `git`, `qt6-qttools`. Plasma Keyboard set via `kwinrc [Wayland] InputMethod`. Passwordless sudo for `adamrel`.
- Gates re-run (section B): 4 pass, 0 fail. Quickshell ≥ 0.3.0, polkit agent is its own unit, virtual keyboard configured; notification bus name still with plasmashell (untested until a NotificationServer exists).
- **OSK probe: GREEN** — keyboard raised in None / OnDemand / Exclusive, text delivered to plain and password fields. `Qt.inputMethod.visible` false and `keyboardRectangle` 0×0 throughout (Plasma Keyboard does not update Qt's IM state) → keep input surfaces top-anchored. Recorded in `quickshell-starlite-rice.md` §3.1.8.
- Note for the probe tooling: `console.log` notes did not reach the qs log file or stdout on this build; the on-screen readout was the source of truth (screenshot via `spectacle -b -n`).
